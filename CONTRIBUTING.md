# 贡献指南

本仓库只接收能长期帮助 AI Agent 编写、核验或排查影刀项目的内容。优先修正现有页面；没有明确独立职责时不要新增文件。

## 事实证据等级

技术结论按以下等级描述：

1. **运行验证**：已在真实影刀环境运行通过，并记录必要的版本或环境。
2. **源码确认**：已从当前扩展、市场指令或可追溯实现中确认。
3. **稳定代码依据**：真实项目或仓库存在可复核的稳定调用，但尚未独立运行验证。
4. **待确认**：只有文档、错误现象或推测，尚无充分证据。

第 4 级不得写成稳定事实。未确认结论如果仍有排查价值，应留在对应 API 页面并明确证据缺口和验证方式。

版本相关事实应写明适用版本或确认日期。无法说明来源的一次性经验、项目业务常量和临时 workaround 不进入公共知识库。

## 内容边界

- 真实影刀项目的跨项目常驻核心约束以 [project-template/AGENTS.md](project-template/AGENTS.md) 为唯一规范源；[编程风格详解](docs/coding-style.md) 只保存示例和低频边界，不削弱项目规则。根 [AGENTS.md](AGENTS.md) 只维护知识库本身的职责、审查和路由规则。
- API 签名、参数、返回值和限制写入对应的 `xbot-api-docs/docs/` 页面。
- 市场指令参数和返回值写入对应事实页；只有新增指令时才在总索引补一条链接。
- 项目专用流程、页面字段和业务口径留在真实项目文档中。
- 旧版本行为只有仍影响当前使用、证据可追溯时才保留；其余交给 Git 历史，不新建归档目录。

## 最小示例

示例只保留证明 API 用法所需的最少内容：

- 使用真实存在的导入、参数和返回结构。
- 删除与目标 API 无关的业务封装、配置和异常兜底。
- 标明运行环境以及哪些部分已验证。
- 同一用法只维护一份；其他页面链接到它，不复制代码。

API Python 片段须注明前置对象和适用条件，不声称可独立运行。脱敏占位、未知步骤和有实际副作用的调用统一使用标注“非执行调用说明（不可直接运行）”的 `text` 代码块或正文；边界遵守 [xbot-api-docs/AGENTS.md](xbot-api-docs/AGENTS.md) 的 API 文档维护规则。

## 脱敏

提交前删除或替换：

- API Key、Secret、Token、Cookie、Webhook 和账号密码。
- 用户 ID、手机号、订单号、客户数据和公司内部地址。
- 本机绝对路径、未公开接口和可识别业务数据。

文档脱敏说明使用 `<client_id>`、`<token>`、`<order_id>` 等占位符，含占位符的示例按上述要求标为非执行说明。日志、截图和返回值同样必须脱敏。

## 链接而不是复制

知识库维护规则引用根 [AGENTS.md](AGENTS.md)，真实项目开发规则引用 [project-template/AGENTS.md](project-template/AGENTS.md)，查找入口引用 [llms.txt](llms.txt)，API 事实引用其唯一页面。不要为了让每个文件“独立完整”而复制规则、教程或长示例。

提交前检查：链接有效、事实等级清楚、示例最小、内容已脱敏、没有新增重复规则。

修改真实项目开发行为的核心约束时，只在 `project-template/AGENTS.md` 维护其正文；不要把同一规则复制回根 `AGENTS.md`。
